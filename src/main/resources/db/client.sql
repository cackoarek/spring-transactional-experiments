CREATE TABLE client (
id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
first_name VARCHAR(255),
last_name VARCHAR(255),
PRIMARY KEY (id)
);

INSERT INTO client VALUES (NULL, 'Tomasz', 'Ciężki');
