CREATE TABLE city (
id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
name VARCHAR(255),
PRIMARY KEY (id)
);

INSERT INTO city VALUES (NULL, 'Warszawa');
INSERT INTO city VALUES (NULL, 'Kraków');

